# Step 8 — (Optional) Connect the MCP server to a Foundry agent  *(placeholder)*

Will cover: a **portal-only** walkthrough — deploy a chat model that
supports MCP tool calling, create an agent in the Foundry portal, add a
Custom MCP Tool pointing at your hosted DAB `/mcp` URL, and chat with
your data.

This step is portal-only on purpose. The SDK path was unreliable when
this tutorial was written.
