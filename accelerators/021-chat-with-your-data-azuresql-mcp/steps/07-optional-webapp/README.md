# Step 7 — (Optional) Web UI for hybrid search  *(placeholder)*

Will cover: a small Streamlit app that calls DAB's REST endpoint (no
direct SQL, no LLM) so you can demo intelligent search to people without
showing them a terminal.
